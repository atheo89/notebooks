---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Baseline images require a separate admin opt-in: apply ./baseline explicitly.
# Keep that overlay out of this resource list and rhoai/base.

labels:
  - includeSelectors: true
    pairs:
      component.opendatahub.io/name: notebooks
      opendatahub.io/component: "true"
